data:
  git:
    name: "{{ promptString \"git_user_name\" }}"
    email: "{{ promptString \"git_user_email\" }}"
  openai:
    api_key: "{{ promptString \"openai_api_key\" }}"
    bytegpt_key: "{{ promptString \"bytegpt_api_key\" }}"
  qiniu:
    access_key: "{{ promptString \"qiniu_access_key\" }}"
    secret_key: "{{ promptString \"qiniu_secret_key\" }}" 